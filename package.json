{
    "name": "stealth-money-monorepo",
    "private": true,
    "workspaces": [
      "packages/*"
    ],
    "scripts": {
      "dev:web": "npm run dev --workspace=web",
      "dev:api": "npm run dev --workspace=api",
      "deploy:contracts": "npm run deploy --workspace=contracts"
    },
    "devDependencies": {
    }
  }